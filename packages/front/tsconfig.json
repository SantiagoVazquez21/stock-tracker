{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // El front corre en el navegador: sumamos las libs del DOM y activamos JSX.
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "jsx": "react-jsx"
  },
  "include": ["src", "vite.config.ts"]
}
